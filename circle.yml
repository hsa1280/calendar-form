machine:
  node:
    version: 7.8.0
test:
  override:
    - "true"
dependencies:
  pre:
    - npm install -g webpack
compile:
  override:
    - npm run build
deployment:
    staging:
      branch: master
      heroku:
        appname: calendar-form
